<form method="post" action="{{ path('distribuidor_delete', {'id': distribuidor.id}) }}" id="deleteDistribuidorForm">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ distribuidor.id) }}">
    <button class="btn btn-danger ms-3 mt-3" type="button" id="delete-distribuidor">Borrar</button>
</form>
