<form method="post" action="{{ path('banner_delete', {'id': banner.id}) }}" id="deleteBannerForm">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ banner.id) }}">
    <button class="btn btn-danger ms-3 mt-3" type="button" onclick="confirmation(document.getElementById('deleteBannerForm'), '¿Está seguro de borrar el banner?')">Borrar</button>
</form>
{% block js %}
    <script>
        function confirmation(form, text) {
            swal({
                title: "Confirmación",
                text: text,
                icon: "info",
                buttons: {
                    cancel: {
                        text: "No",
                        value: null,
                        visible: true,
                        className: "",
                        closeModal: true,
                    },
                    confirm: {
                        text: "Si",
                        value: true,
                        visible: true,
                        className: "",
                        closeModal: true
                    }
                },
            }).then((value) => {
                if (value) {
                    /*swal("Poof! Your imaginary file has been deleted!", {
                        icon: "success",
                    });*/
                    form.submit();
                } else {
                    /*swal("Your imaginary file is safe!");*/
                    return false;
                }
            });
        }

    </script>
{% endblock %}
