<form method="post" action="{{ path('configuration_delete', {'id': configuration.id}) }}" id="deleteConfigurationForm">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ configuration.id) }}">
    <button class="btn btn-danger mt-3" type="button" id="delete-configuration" >Borrar configuración</button>
</form>