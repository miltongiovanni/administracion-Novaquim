{% extends 'auth.html.twig' %}

{% block title %}{{ title }} - {{ web_title }}{% endblock %}

{% block content %}
    <div class="row h-100">
        <div class="col-lg-5 col-12">
            <div id="auth-left">
                <div class="auth-logo">
                    <img src="{{ asset('build/images/logo/LogoNova1.jpg') }}" alt="Logo">
                </div>
                <h1 class="auth-title">Contraseña olvidada</h1>
                <p class="auth-subtitle mb-5">Ingrese su dirección de correo y le enviaremos un código para restablecer
                    su contraseña.</p>

                <form action="{{ path('app_send_passcode') }}" method="post">
                    <div class="form-group position-relative has-icon-left mb-4">
                        <input type="email" class="form-control form-control-xl" placeholder="Correo electrónico" name="email"
                               id="inputEmail" required autofocus>
                        <div class="form-control-icon">
                            <i class="bi bi-envelope"></i>
                        </div>
                    </div>
                    <button class="btn btn-primary btn-block btn-lg shadow-lg mt-5">Enviar</button>
                </form>
                <div class="text-center mt-5 text-lg fs-4">
                    <p class='text-gray-600'>¿Recuerda su cuenta? <a href="{{ path('app_login') }}" class="font-bold">Iniciar
                            sesión</a>
                    </p>
                </div>
            </div>
        </div>
        <div class="col-lg-7 d-none d-lg-block">
            <div id="auth-right">

            </div>
        </div>
    </div>
{% endblock %}
