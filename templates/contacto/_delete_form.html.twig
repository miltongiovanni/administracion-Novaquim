<form method="post" action="{{ path('app_contacto_delete', {'id': contacto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contacto.id) }}">
    <button class="btn">Delete</button>
</form>
