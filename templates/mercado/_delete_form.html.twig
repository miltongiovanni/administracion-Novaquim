<form method="post" action="{{ path('mercado_delete', {'id': mercado.id}) }}" id="deleteMercadoForm">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mercado.id) }}">
    <button class="btn btn-danger mt-3" type="button" id="delete-mercado" >Borrar mercado</button>
</form>