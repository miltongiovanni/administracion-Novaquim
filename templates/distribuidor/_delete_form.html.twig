<form method="post" action="{{ path('distribuidor_delete', {'id': distribuidor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ distribuidor.id) }}">
    <button class="btn">Delete</button>
</form>
