<form method="post" action="{{ path('product_delete', {'id': product.id}) }}" id="deleteProductForm">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <button class="btn btn-danger ms-3 mt-3" type="button" id="delete-producto" >Borrar</button>
</form>